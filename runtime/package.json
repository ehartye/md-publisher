{
  "name": "md-publisher-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Node deps for md-publisher's mermaid pre-render. Installed at ~/.md-publisher/runtime/ on first run.",
  "dependencies": {
    "@mermaid-js/mermaid-cli": "11.4.2"
  }
}
